---
title: Python hub
---

# Python

Master backlog: [**Interview syllabus (master list)**](../interview-syllabus/) — Python section (checklist + links as pages land).

Use this hub for **Python-only** reference pages (stdlib idioms that do not map cleanly to a cross-language topic).

## Topic pages

- [with & context managers (`contextlib`)](../python-context-managers/) — `__enter__` / `__exit__`, `@contextmanager`, `ExitStack`, `async with`

## Ideas for dedicated pages

- dunder methods for iteration (`__iter__` / `__next__`)
- generators and `yield from`

Cross-language iteration concepts often live on [Iterators & generators](../iterators-and-generators/) instead.

## Lesson track

Add ordered modules under `learnings/python/lessons/` when you start that track.
